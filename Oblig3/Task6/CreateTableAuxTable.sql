--Setter ingen pk her siden eventID vil gå igjen frem til at jeg lager forskjellige tabeller for det. 
CREATE TABLE auxTable(
	eventId VARCHAR(10),
	eventTitle VARCHAR(88),
	eventDate DATETIME,
	totSpaces INTEGER,
	pId VARCHAR(7),
	lName VARCHAR (15),
	fName VARCHAR (15)
);

